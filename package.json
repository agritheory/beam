{
	"name": "beam",
	"private": true,
	"repository": {
		"type": "git",
		"url": "https://github.com/agritheory/beam.git"
	},
	"scripts": {
		"install": "cd beam/www/beam && yarn install",
		"dev": "cd beam/www/beam && yarn dev",
		"build": "cd beam/www/beam && yarn build"
	},
	"publishConfig": {
		"access": "restricted"
	}
}
